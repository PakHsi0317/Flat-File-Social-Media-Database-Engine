# Flat-File-Social-Media-Database-Engine
mplemented a lightweight social media storage system in C++ using flat CSV files for users, posts, and engagements. Built in-memory indexes, supported thread-safe parallel loading, durable atomic updates, and referential integrity checks, simulating core database behaviors.
